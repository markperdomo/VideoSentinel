#compdef monitor_queue.py

_arguments -s -S \
    '(-h --help)'{-h,--help}'[Show help message and exit]' \
    '--temp-dir[Custom temp directory]:temp dir:_files -/' \
    '--all[Show all files including completed]' \
    '--failed-only[Only show failed files]'
